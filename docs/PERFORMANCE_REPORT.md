# 效能報告

**日期**：2026-05-02  
**環境**：GitHub Codespaces (Linux x86_64, Python 3.12, debug build)

---

## 回測引擎效能

| 測試場景 | 數據量 | 耗時（暖機後）| 目標 | 結果 |
|---------|--------|------------|------|------|
| 10 年日K線，雙均線策略 | 2,400 筆 | 0.13 ~ 0.16 s | < 5 s | ✅ 通過 |
| 100 次回測迴圈（記憶體） | 485 筆 × 100 次 | — | 無洩漏 | ✅ 通過 |

**備註**：首次 `vbt.Portfolio.from_signals()` 呼叫因 VectorBT 初始化有 ~5s 延遲，
後續呼叫均在 0.2s 以內。正式環境應在應用啟動時預熱。

---

## 記憶體使用量

| 指標 | 數值 |
|------|------|
| 100 次回測峰值記憶體 | 36.7 MB |
| 記憶體增長趨勢 | 0.003 MB / 10 次（無洩漏）|
| Portfolio 物件釋放 | ✅ `del + gc.collect()` 正確釋放 |

---

## Rust vs Python 效能比較

| 指標 | Python | Rust (debug build) | 加速比 |
|------|--------|-------------------|--------|
| Kalman 濾波 (2,400 筆, 1,000 次) | 862.9 μs/次 | 73.3 μs/次 | **11.8x** ✅ |

**目標**：Rust > Python 10x — **達成（11.8x）**  
**備註**：使用 `maturin develop`（debug build）。Release build 預期加速比更高（20x+）。

---

## Rust 邊界穩定性

| 測試項目 | 結果 |
|---------|------|
| 空陣列 → ValueError | ✅ |
| NaN 值 → ValueError（含位置資訊） | ✅ |
| Inf 值 → ValueError | ✅ |
| 極大值 (1e15) | ✅ 正常處理 |
| 極小值 (1e-15) | ✅ 正常處理 |
| Rust panic → RuntimeError（無 segfault）| ✅ |

---

## 結論

- 所有效能目標達成
- 記憶體管理符合規範，無洩漏
- Rust 模組邊界安全，不會導致 Python 進程崩潰
